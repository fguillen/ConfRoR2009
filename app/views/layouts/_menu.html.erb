<div id="main-menu" class="menu">
  <ul>
    <!-- %= menu_to 'Home', root_path % -->
    <!-- %= menu_to 'Calendar', calendar_path % -->
    <!-- %= menu_to 'Talks', papers_path % -->
    <%= menu_to t('views.menu.send_paper'), new_paper_path %>
    <% if admin? %>
      <%= menu_to t('views.menu.events'), events_path %>
    <% end %>
    <!-- %= menu_to 'People', users_path(:page => 1) % -->
    <%= menu_to 'Blog', 'http://conferenciarails.org' %>
    <% StaticPage.all.each do |static_page| %>
      <%= menu_to static_page.title, static_page_path( static_page ) %>
    <% end %>

    <%= menu_to 'English', locale_path( :locale => 'en' )  if I18n.locale.to_s != 'en' %>
    <%= menu_to 'Español', locale_path( :locale => 'es' )  if I18n.locale.to_s != 'es' %>
    
  </ul>
</div>

<div id="user-menu" class="menu">
  <ul>
  <% if logged_in? %>
    <%= menu_to t('views.menu.profile'), user_path(current_user) %>
    <% if current_user.everything_paid? %>
      <!-- %= menu_to 'Pay!', new_cart_path % -->
    <% else %>
      <!-- %= menu_to 'Pay!', new_cart_path % --> <!-- , :highlight => true -->
    <% end %>
    <%= menu_to t('views.menu.logout'), logout_path %>
  <% else %>
    <%= menu_to 'Login', new_session_path %>
    <%= menu_to t('views.menu.signup'), new_user_path %> <!-- , :class => "highlight" -->
  <% end %>
  </ul>
</div>
